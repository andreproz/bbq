<% require 'devise/version' %>
<p><%= t('.greeting', :recipient => @resource.email, :default => "Здравствуйте #{@resource.email}!") %></p>

<p><%= t('password_reset.instruction') %></p>

<p><%= link_to t('form.change_password'), edit_password_url(@resource, :reset_password_token => @token) %></p>

<p><%= t('password_reset.instruction2') %></p>
<p><%= t('password_reset.instruction3') %></p>
